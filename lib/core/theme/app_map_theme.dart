class AppMapTheme {
  /// The default colorful and easy-to-see Google Map theme.
  /// Returning an empty JSON array '[]' overrides any parent styles 
  /// and forces the native colorful default theme of Google Maps.
  static const String defaultStyle = '[]';
}
